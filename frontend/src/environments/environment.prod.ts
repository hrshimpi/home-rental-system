// TODO: set this to the deployed backend's URL before shipping a
// production build - see "Deployment considerations" in DEV_NOTES.md.
export const environment = {
    production: true,
    apiBaseUrl: 'https://REPLACE_ME_WITH_DEPLOYED_BACKEND_URL/'
};
